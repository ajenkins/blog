---
layout: layouts/base.njk
title: AJ's Blog
description: A list of all of the blog posts written by AJ Jenkins.
---

<div id="container">
  <h1>All posts</h1>
  {% for post in collections.post | reverse %}
    <p><a href="{{ post.url | url }}">{{ post.data.title }}</a>
    <span class="publication-date">
      ({{ post.date.toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) }})
    </span></p>
  {% endfor %}
<div>